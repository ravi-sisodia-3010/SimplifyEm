import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.simplifyem',
  appName: 'SimplifyEM',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
